# Public Example Profile

This directory is safe to publish. It contains fake candidate data that shows the shape expected by the app.

For your real use, copy these files into `data/profile/` or edit them through the Personalize tab in the web UI. The real `data/profile/` directory is ignored by Git.
